import { RowDataHandlerDirective } from './row-data-handler.directive';

describe('RowDataHandlerDirective', () => {
  it('should create an instance', () => {
    const directive = new RowDataHandlerDirective();
    expect(directive).toBeTruthy();
  });
});
