import { DynamicStringPipe } from './dynamic-string.pipe';

describe('DynamicStringPipe', () => {
  it('create an instance', () => {
    const pipe = new DynamicStringPipe();
    expect(pipe).toBeTruthy();
  });
});
